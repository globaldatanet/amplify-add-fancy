{
  "name": "<%= props.functionName %>",
  "version": "0.0.1",
  "description": "Lambda function written in Typescript for Amplify",
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "aws-lambda": "^1.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.13.2",
    "typescript": "^4.4.3",
    "@types/aws-lambda": "^8.10.83",
    "@types/node": "^16.9.6"
  },
  "scripts": {
    "tsc": "tsc",
    "build": "esbuild ./index.ts --bundle --minify --platform=node --outdir=./src"
  }
}
