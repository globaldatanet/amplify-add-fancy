"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("../constants");
module.exports = {
    name: constants_1.categoryName,
    run: async (context) => {
        if (/^win/.test(process.platform)) {
            try {
                const { run } = require(`./${constants_1.categoryName}/${context.parameters.first}`);
                return run(context);
            }
            catch (e) {
                context.print.error('Command not found');
            }
        }
        const header = `amplify ${constants_1.categoryName} <subcommands>`;
        const commands = [
            {
                name: 'add',
                description: `Takes you through a CLI flow to add a ${constants_1.categoryName} resource to your local backend`,
            },
            {
                name: 'update',
                description: `Takes you through a CLI flow to update an existing ${constants_1.categoryName} resource`,
            },
            {
                name: 'push',
                description: `Provisions only ${constants_1.categoryName} cloud resources with the latest local developments`,
            },
            {
                name: 'remove',
                description: `Removes ${constants_1.categoryName} resource from your local backend which would be removed from the cloud on the next push command`,
            },
            {
                name: 'build',
                description: 'Builds all the functions in the project (does an npm install on the functions src directory)',
            },
        ];
        context.amplify.showHelp(header, commands);
        context.print.info('');
    },
};
//# sourceMappingURL=function.js.map